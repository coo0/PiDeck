# 钉住 npm 主版本：不同 npm 版本对 peer / bundled 依赖的写法不同，会在
# `npm install` 时静默重写 package-lock.json，造成周期性 lock 漂移
# （历史提交 30e48f1e8 / 3a569ec64 / 6bef3bdac 都在反复同步同一件事）。
#
# engine-strict 让 npm 在版本不匹配时直接拒绝安装，而不是装完再改写 lock。
# 版本约束与 package.json 的 engines.npm 一致（CI 全部使用 node 24.13.0，自带 npm 11.6.2）。
#
# 注意：这里只约束 npm 自身；生成 lock 的改动请一律走 `npm ci`（它从不改写 lock），
# 需要更新依赖时再显式 `npm install` 并单独提交 lock 变更。
engine-strict=true
